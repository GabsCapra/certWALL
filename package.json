{
  "name": "CertWALL",
  "version": "1.0.0",
  "description": "",
  "main": "certwall.js",
  "scripts": {
    "start": "node certwall",
    "publish": "git branch gh-pages && git checkout gh-pages && git add . && git commit -m \"updated gh-pages\" && git push && echo \"Check in your github!\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vmaffioli",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "prompt-sync": "^4.2.0"
  }
}
